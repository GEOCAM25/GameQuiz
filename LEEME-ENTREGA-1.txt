========================================
GAME QUIZ — ENTREGA 1: BUGS + ANTI-ZOOM
========================================

QUÉ CORRIGE (Sección 1 completa + punto 8)
 1. Spoiler: la respuesta correcta ya NO viaja por Realtime antes del reveal.
 2. Nombres duplicados: no deja entrar con un nombre ya usado en la sala.
 3. Congelamiento: nuevo motor "watchdog" — el juego no se cuelga esperando al servidor.
 4. Sin refrescar a mano: el anfitrión repara el avance solo y cada jugador
    re-sincroniza automáticamente cada 4s si Realtime se cae.
 5. Avanza al instante cuando TODOS responden (no espera los 40s).
 6. "Jugar otra ronda": botón nuevo en el podio (solo anfitrión) que reinicia
    puntajes y vuelve al lobby sin que la app se pegue.
 7. Si el anfitrión sale, otro jugador toma el mando automáticamente.
 8. Anti doble-tap-zoom en toda la interfaz (iOS incluido).

CÓMO INSTALARLO (2 pasos)
 PASO 1 — Base de datos (hazlo PRIMERO):
   Supabase → SQL Editor → New query → pega TODO "migracion-1.sql" → Run.
   (Agrega la columna phase_until que el motor necesita. Si ya existe, no pasa nada.)

 PASO 2 — Sube a GitHub reemplazando, respetando carpetas:
   - index.html        (raíz)
   - css/style.css     (carpeta css)
   - js/app.js         (carpeta js)
   NO toques tu js/config.js. NO se toca la carpeta data (preguntas).

 Espera 1-2 min a que GitHub Pages actualice y prueba.

NOTAS
 - "supabase-setup.sql" viene actualizado por si algún día recreas la base
   desde cero. Si tu base ya funciona, solo necesitas migracion-1.sql.
 - Pendiente próximas entregas: UI (imágenes grandes, colores de avatar, fondos
   por categoría), puntajes por sala, compartir, propina y mini-juegos.
 - Duda de "Delator": ¿el votado pierde 15 pts fijos o 15 por cada voto?
   La necesito resuelta para la entrega de mini-juegos.
