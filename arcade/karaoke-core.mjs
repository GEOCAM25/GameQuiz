export function parseVideo(value){try{const s=value.trim();if(/^[\w-]{11}$/.test(s))return s;const u=new URL(s);if(!['youtube.com','www.youtube.com','m.youtube.com','youtu.be','www.youtube-nocookie.com'].includes(u.hostname))return null;const id=u.hostname==='youtu.be'?u.pathname.slice(1):u.searchParams.get('v')||u.pathname.match(/^\/(?:embed|shorts)\/([^/]+)/)?.[1];return /^[\w-]{11}$/.test(id||'')?id:null;}catch{return null;}}
export function parseLRC(text){const lines=[];for(const line of text.split(/\r?\n/)){const tags=[...line.matchAll(/\[(\d+):(\d{2})(?:\.(\d{1,3}))?\]/g)];const words=line.replace(/\[[^\]]*\]/g,'').trim();for(const m of tags){const t=+m[1]*60+(+m[2])+Number('0.'+(m[3]||'0'));if(words&&+m[2]<60)lines.push({t,text:words});}}return lines.sort((a,b)=>a.t-b.t);}
